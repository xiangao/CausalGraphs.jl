var documenterSearchIndex = {"docs":
[{"category":"section","location":"vignettes/Nested_and_Missing_Data/#Nested-Fixable-Estimation-and-Missing-Data","page":"Nested-Fixable and Missing Data","text":"This vignette shows the two pieces that go beyond standard backdoor/front-door workflows: nested-fixable effects and missing-data weighting. All datasets are simulated.\n\nThe common theme is identification before estimation. The graph determines whether the target law can be represented using observed data. Only after that step does the package construct weights and nuisance regressions.","title":"Nested-Fixable Estimation and Missing Data"},{"category":"section","location":"vignettes/Nested_and_Missing_Data/#Nested-Fixable-Effects","page":"Nested-Fixable and Missing Data","text":"A treatment is nested-fixable when the target effect is identified by a One-Line ID style fixing sequence, but the treatment is neither a-fixable nor p-fixable. This happens in ADMGs where hidden-confounding structure is too complex for ordinary backdoor adjustment or a front-door/NPS argument.\n\nThe word \"nested\" comes from nested Markov models for ADMGs. Identification is described by repeatedly \"fixing\" variables: moving a variable from random to fixed status and updating the kernel for the remaining random variables. If a valid fixing sequence isolates the target counterfactual distribution, the effect is identified.\n\nusing CausalGraphs, DataFrames, Random\n\ngraph = make_graph(\n    vertices = [:ViralLoad, :Income, :Exercise, :T, :Toxicity, :Adherence, :CD4],\n    di_edges = [\n        (:ViralLoad, :T), (:ViralLoad, :CD4),\n        (:Income, :T), (:Exercise, :CD4),\n        (:T, :Toxicity), (:T, :CD4),\n        (:Toxicity, :Adherence), (:Adherence, :CD4),\n    ],\n    bi_edges = [\n        (:Income, :Toxicity),\n        (:Exercise, :Income),\n        (:Exercise, :T),\n        (:ViralLoad, :Adherence),\n        (:ViralLoad, :CD4),\n    ],\n)\n\nidentify(graph, :T, :CD4).strategy\n\n(district_T = district(graph, :T),\n children_T = children(graph, :T),\n p_fixable = is_p_fix(graph, :T))\n\ndraw_graph(graph; direction=\"TB\")\n\nFor nested-fixable effects, estimate_causal() returns augmented nested IPW (ANIPW) and nested IPW (NIPW) estimates.\n\nNested rebalancing weights are analogous to ordinary IPW, but they balance the pieces of the observed law that appear in the nested identification formula. ANIPW augments those weights with outcome regression terms, in the same spirit that AIPW augments ordinary IPW.\n\nIn this example, Toxicity is both a child of T and in the same district as T, so p-fixability fails. The nested route is needed because the effect is identified by a fixing sequence rather than by one adjustment set or one front-door mediator formula.\n\nRandom.seed!(42)\nn = 500\nU_EI = randn(n); U_ET = randn(n); U_ITox = randn(n)\nU_VA = randn(n); U_VCD4 = randn(n)\nViralLoad = randn(n)\nExercise  = randn(n) .+ 0.5 .* U_EI .+ 0.5 .* U_ET\nIncome    = randn(n) .+ 0.4 .* ViralLoad .+ 0.4 .* U_EI .+ 0.4 .* U_ITox\nT         = Float64.(rand(n) .< 1 ./(1 .+ exp.(-(0.5 .* ViralLoad .+ 0.3 .* Income .+ 0.4 .* U_ET))))\nToxicity  = randn(n) .+ 0.6 .* T .+ 0.3 .* U_ITox\nAdherence = randn(n) .+ 0.5 .* Toxicity .+ 0.3 .* U_VA\nCD4       = randn(n) .+ 0.4 .* T .+ 0.3 .* Adherence .+\n            0.2 .* Exercise .+ 0.3 .* ViralLoad .+ 0.3 .* U_VCD4 .+ 0.2 .* U_VA\ndf = DataFrame(ViralLoad=ViralLoad, Income=Income, Exercise=Exercise,\n               T=T, Toxicity=Toxicity, Adherence=Adherence, CD4=CD4)\n\nres = estimate_causal(a=[1, 0], data=df, graph=graph, treatment=:T, outcome=:CD4)\nr = x -> round(x, sigdigits=4)\n(ACE=r(res[:ANIPW].ACE), lower_ci=r(res[:ANIPW].lower_ci), upper_ci=r(res[:ANIPW].upper_ci))","title":"Nested-Fixable Effects"},{"category":"section","location":"vignettes/Nested_and_Missing_Data/#Missing-Data-Weighting","page":"Nested-Fixable and Missing Data","text":"An mDAG extends a causal graph with missingness indicators. A variable such as X has an indicator Rx showing whether X is observed. Edges into Rx encode the missingness mechanism.\n\nThe key identification question is whether the target full-data law can be recovered from the observed-data law. If it can, the package estimates observation probabilities and forms inverse-probability weights. Those weights can be passed to estimate_causal(...; sample_weights=wts) so complete cases are weighted back toward the target population.\n\nThis separates two assumptions: the causal ADMG describes treatment, outcome, and covariates; the mDAG describes how values become missing.\n\nFor a simple MAR graph, if the probability of observing X depends only on fully observed variables, complete cases can be weighted by the inverse observation probability. If missingness depends directly on the unobserved value of X, the target law may not be recoverable without additional graph structure.\n\nmdag = make_mdag(\n    obs_variables = [\"A\", \"Y\"],\n    missing_variables = [\"X\"],\n    missing_indicators = [\"Rx\"],\n    di_edges = [(\"X\", \"A\"), (\"X\", \"Y\"), (\"X\", \"Rx\"), (\"A\", \"Y\")],\n)\n\nID = ID_algorithm(mdag)\nID.target.id_status\n\ndraw_graph(mdag)\n\ncompute_missing_weights() estimates the identified missingness propensities and returns inverse-probability weights that can be passed to estimate_causal(...; sample_weights=wts).","title":"Missing-Data Weighting"},{"category":"section","location":"vignettes/Job_Search_Mediation_JOBS/#Job-Search-Mediation-JOBS-II","page":"Job Search Mediation JOBS II","text":"This vignette uses the JOBS II job-search intervention data from the mediation R package, mirrored by RDatasets. JOBS II was a randomized field experiment for unemployed workers. The intervention offered job-skills workshops that taught job-search techniques and coping strategies for setbacks in the job-search process. The follow-up data record post-treatment depressive symptoms and employment-related measures. The full Quarto source is in vignettes/Job_Search_Mediation_JOBS.qmd.","title":"Job Search Mediation JOBS II"},{"category":"section","location":"vignettes/Job_Search_Mediation_JOBS/#Causal-Question","page":"Job Search Mediation JOBS II","text":"How much of the effect of a job-skills workshop on depressive symptoms is mediated by job-search self-efficacy?\n\nThe treatment is treat, the mediator is job_seek, the outcome is depress2, and the baseline covariates are economic hardship, baseline depression, sex, age, and nonwhite race.","title":"Causal Question"},{"category":"section","location":"vignettes/Job_Search_Mediation_JOBS/#Data-and-Research-Idea","page":"Job Search Mediation JOBS II","text":"The research idea is economic as well as behavioral. A job-search program might affect mental health because it changes employment prospects, but it might also work by changing how participants approach the job search itself. The mediator job_seek captures job-search self-efficacy: whether participants feel more capable of conducting a successful search.\n\nThe RDatasets version has 899 complete observations and is intended as an illustrative analysis file. Here the goal is to show a complete causal workflow: data, graph, identification, total-effect estimation, and a clearly labeled mediation decomposition.","title":"Data and Research Idea"},{"category":"section","location":"vignettes/Job_Search_Mediation_JOBS/#Mediation-DAG","page":"Job Search Mediation JOBS II","text":"Let A be workshop assignment, M be job-search self-efficacy, Y be post-treatment depressive symptoms, and X be baseline variables.\n\nX -> A\nX -> M\nX -> Y\nA -> M\nM -> Y\nA -> Y\n\nusing CausalGraphs\n\nconceptual_graph = make_graph(\n    vertices = [:X, :A, :M, :Y],\n    di_edges = [\n        (:X, :A), (:X, :M), (:X, :Y),\n        (:A, :M), (:M, :Y), (:A, :Y),\n    ],\n)\n\ndraw_graph(conceptual_graph; direction=\"LR\")\n\nFor estimation, expand X into the observed baseline columns.\n\nbaseline = [:econ_hard, :depress1, :sex, :age, :nonwhite]\nvertices = [:treat, baseline..., :job_seek, :depress2]\n\nedges = vcat(\n    [(x, :treat) for x in baseline],\n    [(x, :job_seek) for x in baseline],\n    [(x, :depress2) for x in baseline],\n    [(:treat, :job_seek), (:job_seek, :depress2), (:treat, :depress2)],\n)\n\nmediation_graph = make_graph(vertices=vertices, di_edges=edges)\nidentify(mediation_graph, :treat, :depress2).strategy\n\ndraw_graph(mediation_graph; direction=\"LR\")\n\nmarkov_pillow(mediation_graph, :treat; treatment=:treat)\n\nFor the total effect of treat on depress2, the mediator is part of the causal pathway and is not adjusted away. The graph says that adjustment for the baseline variables is sufficient.","title":"Mediation DAG"},{"category":"section","location":"vignettes/Job_Search_Mediation_JOBS/#Total-Effect-Estimation","page":"Job Search Mediation JOBS II","text":"using DataFrames, DelimitedFiles, Downloads, Statistics\n\nfunction read_jobs()\n    url = \"https://vincentarelbundock.github.io/Rdatasets/csv/mediation/jobs.csv\"\n    local_file = Downloads.download(url; timeout=120)\n    x, header = readdlm(local_file, ',', Any, '\\n'; header=true)\n    raw = DataFrame(x, Symbol.(vec(header)))\n\n    DataFrame(\n        treat = Float64.(raw.treat),\n        econ_hard = Float64.(raw.econ_hard),\n        depress1 = Float64.(raw.depress1),\n        sex = Float64.(raw.sex),\n        age = Float64.(raw.age),\n        nonwhite = Float64.(raw.nonwhite .== \"non.white1\"),\n        job_seek = Float64.(raw.job_seek),\n        depress2 = Float64.(raw.depress2),\n    )\nend\n\ndata = read_jobs()\n\n(n = nrow(data),\n treated = Int(sum(data.treat .== 1)),\n controls = Int(sum(data.treat .== 0)))\n\ntotal_res = estimate_causal(\n    a = [1, 0],\n    data = data,\n    graph = mediation_graph,\n    treatment = :treat,\n    outcome = :depress2,\n)\n\nr = x -> round(x, sigdigits=4)\n\n(TMLE_total_effect = r(total_res[:TMLE].ACE),\n lower_ci = r(total_res[:TMLE].lower_ci),\n upper_ci = r(total_res[:TMLE].upper_ci))\n\nLower values of depress2 correspond to fewer depressive symptoms. Negative effects therefore mean reductions in depressive symptoms. In this simple analysis, the total-effect confidence interval crosses zero.","title":"Total Effect Estimation"},{"category":"section","location":"vignettes/Job_Search_Mediation_JOBS/#Mediation-Decomposition","page":"Job Search Mediation JOBS II","text":"For mediation, define M0 as job-search self-efficacy under no workshop, M1 as job-search self-efficacy under the workshop, and Y(a, m) as depressive symptoms under assignment a and mediator value m.\n\ntotal effect            = E[Y(1, M1) - Y(0, M0)]\nnatural direct effect   = E[Y(1, M0) - Y(0, M0)]\nnatural indirect effect = E[Y(1, M1) - Y(1, M0)]\n\nThe graph-based total effect above only requires identifying the intervention do(A=a). Natural direct and indirect effects require additional assumptions: no unmeasured treatment-outcome, treatment-mediator, or mediator-outcome confounding, plus the usual cross-world condition needed for natural effects.\n\nThe following code computes a parametric mediation g-formula:\n\nusing LinearAlgebra\n\nfunction mediator_matrix(d, avec)\n    n = nrow(d)\n    hcat(ones(n), avec, d.econ_hard, d.depress1, d.sex, d.age, d.nonwhite)\nend\n\nfunction outcome_matrix(d, avec, mvec)\n    n = nrow(d)\n    hcat(\n        ones(n), avec, mvec, avec .* mvec,\n        d.econ_hard, d.depress1, d.sex, d.age, d.nonwhite,\n    )\nend\n\nn = nrow(data)\nzeros_a = zeros(n)\nones_a = ones(n)\n\nmediator_coef = mediator_matrix(data, data.treat) \\ data.job_seek\nm0 = mediator_matrix(data, zeros_a) * mediator_coef\nm1 = mediator_matrix(data, ones_a) * mediator_coef\n\noutcome_coef = outcome_matrix(data, data.treat, data.job_seek) \\ data.depress2\n\ney_1m1 = mean(outcome_matrix(data, ones_a, m1) * outcome_coef)\ney_1m0 = mean(outcome_matrix(data, ones_a, m0) * outcome_coef)\ney_0m0 = mean(outcome_matrix(data, zeros_a, m0) * outcome_coef)\n\ntotal_effect = ey_1m1 - ey_0m0\nnatural_direct = ey_1m0 - ey_0m0\nnatural_indirect = ey_1m1 - ey_1m0\nprop_mediated = natural_indirect / total_effect\n\n(ey_1m1 = r(ey_1m1),\n ey_1m0 = r(ey_1m0),\n ey_0m0 = r(ey_0m0),\n total_effect = r(total_effect),\n natural_direct = r(natural_direct),\n natural_indirect = r(natural_indirect),\n prop_mediated = r(prop_mediated))\n\nIn this linear plug-in analysis, most of the estimated reduction in depressive symptoms is direct. About one fifth of the estimated reduction is mediated through job-search self-efficacy.","title":"Mediation Decomposition"},{"category":"section","location":"vignettes/Job_Search_Mediation_JOBS/#Optional-Crumble.jl-Handoff","page":"Job Search Mediation JOBS II","text":"This vignette does not need Crumble.jl to make the main point. The purpose here is to show how CausalGraphs.jl moves from a DAG to total-effect identification and estimation, and then to show the extra assumptions behind a simple natural-effect mediation decomposition.\n\nCrumble.jl is the better tool when the mediation estimand itself is the main target. It implements mediation estimators for natural, organic, randomized interventional, and recanting-twins effects, with nuisance estimation machinery that is beyond the scope of this graph vignette. For the simple JOBS graph above, the analogous Crumble.jl call would target natural effects:\n\nusing Crumble\n\ncrumble_res = crumble(\n    data,\n    [\"treat\"];\n    outcome = \"depress2\",\n    mediators = [\"job_seek\"],\n    covar = string.(baseline),\n    effect = \"N\",\n)\n\nThis is intentionally shown as optional code rather than a required dependency of CausalGraphs.jl. The graph package should be usable without pulling in a full mediation-estimation stack. In a substantive mediation analysis, however, one natural workflow is to use CausalGraphs.jl to state and stress-test the DAG assumptions, use identify() to separate total-effect identification from mechanism assumptions, and then use Crumble.jl when the target is a formal mediation estimand and the data support the required assumptions.\n\nThe distinction matters in the next section. If the mediator-outcome relationship is confounded by an unmeasured variable, the total effect may still be identifiable from the graph, but the natural direct and indirect effects are no longer justified by the simple natural-effect formula. A different mediation estimand or additional measured mediator-outcome confounders would be needed before using a richer mediation estimator.","title":"Optional Crumble.jl Handoff"},{"category":"section","location":"vignettes/Job_Search_Mediation_JOBS/#Mediator-Outcome-Confounding","page":"Job Search Mediation JOBS II","text":"The weakest link in many mediation analyses is the mediator-outcome relationship. Job-search self-efficacy is post-treatment, not randomized. Unmeasured optimism, family support, health, or local job-market conditions could affect both self-efficacy and depressive symptoms.\n\nA sensitivity graph adds a bidirected edge between the mediator and outcome. This is different from the directed edge job_seek -> depress2, which is the causal mediated pathway. The bidirected edge job_seek <-> depress2 means there is an unmeasured common cause of job-search self-efficacy and depressive symptoms.\n\nsensitivity_graph = make_graph(\n    vertices = vertices,\n    di_edges = edges,\n    bi_edges = [(:job_seek, :depress2)],\n)\n\ndraw_graph(sensitivity_graph; direction=\"LR\")\n\nThe total effect of treat on depress2 is still a treatment-effect question and can still be estimated under the baseline-adjustment assumptions above, because the total effect does not condition on or intervene on the mediator. CausalGraphs.jl therefore still identifies the total effect in this sensitivity graph:\n\nidentify(sensitivity_graph, :treat, :depress2).strategy\n\nmarkov_pillow(sensitivity_graph, :treat; treatment=:treat)\n\nsensitivity_total_res = estimate_causal(\n    a = [1, 0],\n    data = data,\n    graph = sensitivity_graph,\n    treatment = :treat,\n    outcome = :depress2,\n)\n\n(TMLE_total_effect = r(sensitivity_total_res[:TMLE].ACE),\n lower_ci = r(sensitivity_total_res[:TMLE].lower_ci),\n upper_ci = r(sensitivity_total_res[:TMLE].upper_ci))\n\nThe natural direct and indirect effects, however, are no longer justified by the simple mediation g-formula because the M -> Y relationship is confounded after conditioning on A and X.","title":"Mediator-Outcome Confounding"},{"category":"section","location":"vignettes/Job_Search_Mediation_JOBS/#Takeaway","page":"Job Search Mediation JOBS II","text":"This example separates three tasks that are often collapsed in applied mediation work:\n\nthe DAG encodes the economic and behavioral story;\nidentify() and estimate_causal() handle the total effect of the workshop;\nthe natural-effect decomposition requires extra assumptions and an explicit mediation formula;\nCrumble.jl is the natural companion when the mediation estimand, rather than the graph-to-total-effect workflow, is the main analysis target.","title":"Takeaway"},{"category":"section","location":"vignettes/Job_Training_NSW/#Job-Training-and-Earnings","page":"Job Training NSW","text":"This vignette summarizes the National Supported Work (NSW) job-training example. NSW was a social experiment from the 1970s that targeted disadvantaged workers. Treated participants were offered a supported work placement for a limited period, and later earnings were measured from administrative records. The data are mirrored by RDatasets. The full Quarto source is in vignettes/Job_Training_NSW.qmd.","title":"Job Training and Earnings"},{"category":"section","location":"vignettes/Job_Training_NSW/#Causal-Question","page":"Job Training NSW","text":"What is the effect of job-training participation on real earnings in 1978?\n\nThe treatment is treat, the outcome is re78, and the baseline covariates are age, education, race/ethnicity indicators, marital status, degree status, and pre-treatment real earnings in 1974 and 1975.","title":"Causal Question"},{"category":"section","location":"vignettes/Job_Training_NSW/#Data-and-Research-Idea","page":"Job Training NSW","text":"The research idea is the classic program-evaluation problem: can an employment and training program raise later earnings? NSW is useful because it connects an experimental benchmark to the observational problem economists often face. If assignment is randomized, a simple comparison can estimate the effect. If participation is analyzed as if it came from a non-random labor market or administrative process, baseline earnings, education, age, race, marital status, and degree status become part of the identifying story.\n\nThe RDatasets file used here has 445 observations and 11 variables. The analysis below keeps the variables needed for the treatment, outcome, and baseline adjustment set.","title":"Data and Research Idea"},{"category":"section","location":"vignettes/Job_Training_NSW/#Experimental-Graph","page":"Job Training NSW","text":"If the NSW sample is treated as a randomized experiment, the graph is:\n\nA -> Y\n\nusing CausalGraphs\n\nexperimental_graph = make_graph(\n    vertices = [:treat, :re78],\n    di_edges = [(:treat, :re78)],\n)\n\nidentify(experimental_graph, :treat, :re78).strategy\n\ndraw_graph(experimental_graph; direction=\"LR\")\n\nmarkov_pillow(experimental_graph, :treat; treatment=:treat)\n\nNo baseline adjustment is required under this graph.","title":"Experimental Graph"},{"category":"section","location":"vignettes/Job_Training_NSW/#Observational-Selection-Graph","page":"Job Training NSW","text":"For an observational analysis, the graph allows measured baseline variables to affect both program participation and later earnings:\n\nX -> A -> Y\nX -----> Y\n\nconceptual_graph = make_graph(\n    vertices = [:X, :A, :Y],\n    di_edges = [(:X, :A), (:X, :Y), (:A, :Y)],\n)\n\ndraw_graph(conceptual_graph; direction=\"LR\")\n\nvertices = [\n    :age, :educ, :black, :hisp, :marr, :nodegree,\n    :re74, :re75, :treat, :re78,\n]\n\nbaseline = setdiff(vertices, [:treat, :re78])\nedges = vcat(\n    [(w, :treat) for w in baseline],\n    [(w, :re78) for w in baseline],\n    [(:treat, :re78)],\n)\n\nobservational_graph = make_graph(vertices=vertices, di_edges=edges)\nidentify(observational_graph, :treat, :re78).strategy\n\ndraw_graph(observational_graph; direction=\"LR\")\n\nmarkov_pillow(observational_graph, :treat; treatment=:treat)\n\nUnder this graph, the effect is identified by adjusting for the measured baseline covariates.","title":"Observational Selection Graph"},{"category":"section","location":"vignettes/Job_Training_NSW/#Unmeasured-Selection","page":"Job Training NSW","text":"If unmeasured motivation, caseworker discretion, health, networks, or local labor-market opportunity confound training and later earnings, encode that as a bidirected edge:\n\nX -> A -> Y\nX -----> Y\nA <-> Y\n\nselection_graph = make_graph(\n    vertices = vertices,\n    di_edges = edges,\n    bi_edges = [(:treat, :re78)],\n)\n\nidentify(selection_graph, :treat, :re78).strategy\n\ndraw_graph(selection_graph; direction=\"LR\")\n\nWith that additional assumption, CausalGraphs.jl reports the effect as not identified by the implemented criteria.","title":"Unmeasured Selection"},{"category":"section","location":"vignettes/Job_Training_NSW/#Estimation","page":"Job Training NSW","text":"The NSW data can be loaded directly from RDatasets. The estimate under the experimental graph uses only treat and re78, while the observational graph also adjusts for the baseline covariates through the Markov pillow of treat.\n\nusing DataFrames, DelimitedFiles, Downloads, Statistics\n\nfunction read_rdatasets_csv(url, cols)\n    local_file = Downloads.download(url; timeout=120)\n    x, header = readdlm(local_file, ',', Any, '\\n'; header=true)\n    raw = DataFrame(x, Symbol.(vec(header)))\n    DataFrame((c => Float64.(raw[!, c]) for c in cols)...)\nend\n\nurl = \"https://vincentarelbundock.github.io/Rdatasets/csv/causaldata/nsw_mixtape.csv\"\ncols = [\n    :treat, :age, :educ, :black, :hisp, :marr, :nodegree,\n    :re74, :re75, :re78,\n]\n\ndata = read_rdatasets_csv(url, cols)\n\n(n = nrow(data),\n treated = Int(sum(data.treat .== 1)),\n controls = Int(sum(data.treat .== 0)))\n\nUnder the experimental graph, no baseline adjustment is made.\n\nraw_difference = mean(data.re78[data.treat .== 1]) -\n                 mean(data.re78[data.treat .== 0])\n\nexperimental_res = estimate_causal(\n    a = [1, 0],\n    data = select(data, [:treat, :re78]),\n    graph = experimental_graph,\n    treatment = :treat,\n    outcome = :re78,\n)\n\nr = x -> round(x, sigdigits=4)\n\n(raw_difference = r(raw_difference),\n TMLE_ACE = r(experimental_res[:TMLE].ACE),\n lower_ci = r(experimental_res[:TMLE].lower_ci),\n upper_ci = r(experimental_res[:TMLE].upper_ci))\n\nUnder the observational selection-on-observables graph, the estimator adjusts for the measured baseline variables.\n\nobservational_res = estimate_causal(\n    a = [1, 0],\n    data = data,\n    graph = observational_graph,\n    treatment = :treat,\n    outcome = :re78,\n)\n\n(TMLE_ACE = r(observational_res[:TMLE].ACE),\n lower_ci = r(observational_res[:TMLE].lower_ci),\n upper_ci = r(observational_res[:TMLE].upper_ci),\n Onestep_ACE = r(observational_res[:Onestep].ACE),\n Gcomp_ACE = r(observational_res[:Gcomp].ACE),\n IPW_ACE = r(observational_res[:IPW].ACE))\n\nThe key point is not the small numerical difference in this sample. It is that each estimate is tied to a different graph. Under the unmeasured-selection graph above, the effect is not identified, so estimate_causal should not be run.","title":"Estimation"},{"category":"section","location":"vignettes/Job_Training_NSW/#Takeaway","page":"Job Training NSW","text":"The NSW example is useful because it makes the identifying assumptions visible:\n\nrandomized assignment implies no adjustment;\nmeasured selection implies adjustment for baseline covariates;\nunmeasured selection makes the effect non-identified in this ADMG.","title":"Takeaway"},{"category":"section","location":"vignettes/Estimation_Backdoor_Frontdoor/#Estimation:-Backdoor-and-Front-Door","page":"Estimation: Backdoor and Front-Door","text":"This vignette covers the common estimate_causal() workflows for backdoor and front-door style graphs. All datasets are simulated with known true effects.\n\nThe workflow is: encode assumptions in an ADMG, call identify(), then call estimate_causal() only when the graph identifies the target effect. The estimand here is the average causal effect E[Y(1)-Y(0)].","title":"Estimation: Backdoor and Front-Door"},{"category":"section","location":"vignettes/Estimation_Backdoor_Frontdoor/#Backdoor","page":"Estimation: Backdoor and Front-Door","text":"Backdoor identification applies when observed pre-treatment variables block all noncausal paths from treatment to outcome. In this graph, X affects both A and Y, so a raw treated-control comparison is confounded. The adjustment formula is:\n\nE[Y(a)] = sum_x E[Y | A=a, X=x] P(X=x)\n\nThe a-fixable check is the ADMG version of this adjustment logic. The package uses the treatment's Markov pillow as the adjustment set.\n\nIn this example, the only noncausal path from A to Y is A <- X -> Y. Conditioning on X blocks that path. After conditioning on X, treatment variation is treated as if randomized within levels of X; averaging E[Y | A=a, X] over the observed distribution of X gives E[Y(a)].\n\nusing CausalGraphs, DataFrames, Random, Statistics\n\nRandom.seed!(1)\nn = 500\nX = randn(n)\nA = Float64.(rand(n) .< 1 ./ (1 .+ exp.(-X)))\nY = 2 .* A .+ X .+ randn(n)\ndata = DataFrame(X=X, A=A, Y=Y)\n\ngraph = make_graph(\n    vertices = [:X, :A, :Y],\n    di_edges = [(:X, :A), (:X, :Y), (:A, :Y)],\n)\n\nidentify(graph, :A, :Y).strategy\n\ndraw_graph(graph)\n\nres = estimate_causal(\n    a = [1, 0],\n    data = data,\n    graph = graph,\n    treatment = :A,\n    outcome = :Y,\n)\n\nraw = mean(data.Y[data.A .== 1]) - mean(data.Y[data.A .== 0])\nr = x -> round(x, sigdigits=4)\n(unadjusted = r(raw),\n ACE = r(res[:TMLE].ACE),\n lower_ci = r(res[:TMLE].lower_ci),\n upper_ci = r(res[:TMLE].upper_ci))\n\nThe true ACE is 2.0. For an a-fixable treatment, estimate_causal() uses the Markov pillow of the treatment as the adjustment set and returns TMLE, one-step/AIPW, G-computation, and IPW estimates.\n\nG-computation models the outcome regression. IPW models the propensity score. One-step/AIPW and TMLE combine both nuisance models; TMLE additionally updates the outcome regression to solve the efficient influence-function equation.","title":"Backdoor"},{"category":"section","location":"vignettes/Estimation_Backdoor_Frontdoor/#Front-Door-/-P-Fixable","page":"Estimation: Backdoor and Front-Door","text":"Front-door identification is useful when treatment and outcome are confounded, but the causal effect flows through observed post-treatment variables. The bidirected edge A <-> Y means ordinary backdoor adjustment fails. The mediator M helps because the graph identifies how A changes M and how M changes Y.\n\nThe classical front-door functional is:\n\nE[Y(a)] = sum_m P(M=m | A=a) sum_a' E[Y | M=m, A=a'] P(A=a')\n\nThe package uses the broader p-fixable/NPS route. A treatment is p-fixable when none of its children are in its bidirected-connected district. Intuitively, treatment may be confounded with the outcome, but its immediate causal children are not hidden-confounded with treatment.\n\nHere A is in the same district as Y, so backdoor adjustment fails. But M, the child of A, is not in that district. The graph identifies the effect by combining the treatment-mediator relationship with the mediator-outcome relationship while averaging over treatment to remove the A <-> Y confounding.\n\nRandom.seed!(2)\nn = 700\nA = Float64.(rand(n) .< 0.5)\nM = 0.8 .* A .+ randn(n)\nY = 1.5 .* M .+ randn(n)\nfd_data = DataFrame(A=A, M=M, Y=Y)\n\nfd_graph = make_graph(\n    vertices = [:A, :M, :Y],\n    di_edges = [(:A, :M), (:M, :Y)],\n    bi_edges = [(:A, :Y)],\n)\n\nidentify(fd_graph, :A, :Y).strategy\n\n(district_A = district(fd_graph, :A),\n children_A = children(fd_graph, :A),\n p_fixable = is_p_fix(fd_graph, :A))\n\ndraw_graph(fd_graph)\n\nfd_res = estimate_causal(\n    a = [1, 0],\n    data = fd_data,\n    graph = fd_graph,\n    treatment = :A,\n    outcome = :Y,\n)\n\nr = x -> round(x, sigdigits=4)\n(ACE=r(fd_res[:TMLE].ACE), lower_ci=r(fd_res[:TMLE].lower_ci), upper_ci=r(fd_res[:TMLE].upper_ci))\n\nThe true ACE is 1.2 (= 0.8 × 1.5, by the front-door formula). The package routes to the NPS/front-door style TMLE for p-fixable graphs.","title":"Front-Door / P-Fixable"},{"category":"section","location":"vignettes/Smoking_Cessation_NHEFS/#Smoking-Cessation-and-Weight-Change","page":"Smoking Cessation NHEFS","text":"This vignette summarizes the NHEFS smoking-cessation example. The full Quarto source is in vignettes/Smoking_Cessation_NHEFS.qmd.","title":"Smoking Cessation and Weight Change"},{"category":"section","location":"vignettes/Smoking_Cessation_NHEFS/#Causal-Question","page":"Smoking Cessation NHEFS","text":"What is the effect of quitting smoking on weight change from 1971 to 1982?\n\nThe outcome is wt82_71, the treatment is qsmk, and the baseline covariates include demographics, education, smoking history, activity, exercise, and baseline weight.","title":"Causal Question"},{"category":"section","location":"vignettes/Smoking_Cessation_NHEFS/#Conceptual-Graph","page":"Smoking Cessation NHEFS","text":"The main adjustment graph is:\n\nX -> A -> Y\nX -----> Y\n\nwhere X is the baseline covariate set, A is quitting smoking, and Y is later weight change. This graph is an assumption, not a fact learned from the data.\n\nusing CausalGraphs\n\nconceptual_graph = make_graph(\n    vertices = [:X, :A, :Y],\n    di_edges = [(:X, :A), (:X, :Y), (:A, :Y)],\n)\n\nidentify(conceptual_graph, :A, :Y).strategy\n\ndraw_graph(conceptual_graph)","title":"Conceptual Graph"},{"category":"section","location":"vignettes/Smoking_Cessation_NHEFS/#Expanded-Graph","page":"Smoking Cessation NHEFS","text":"For estimation, X is expanded into actual data columns:\n\nvertices = [\n    :sex, :race, :age, :school,\n    :smokeintensity, :smokeyrs,\n    :exercise, :active, :wt71,\n    :qsmk, :wt82_71,\n]\n\nbaseline = setdiff(vertices, [:qsmk, :wt82_71])\nedges = vcat(\n    [(w, :qsmk) for w in baseline],\n    [(w, :wt82_71) for w in baseline],\n    [(:qsmk, :wt82_71)],\n)\n\ngraph = make_graph(vertices=vertices, di_edges=edges)\nidentify(graph, :qsmk, :wt82_71).strategy\n\ndraw_graph(graph; direction=\"TB\")","title":"Expanded Graph"},{"category":"section","location":"vignettes/Smoking_Cessation_NHEFS/#Sensitivity-Graph","page":"Smoking Cessation NHEFS","text":"If we add an unmeasured common cause of quitting and weight change, the effect is not identified by the implemented criteria:\n\nsensitivity_graph = make_graph(\n    vertices = vertices,\n    di_edges = edges,\n    bi_edges = [(:qsmk, :wt82_71)],\n)\n\nidentify(sensitivity_graph, :qsmk, :wt82_71).strategy\n\ndraw_graph(sensitivity_graph; direction=\"TB\")\n\nThe purpose of the DAG is to make this assumption explicit before estimation.","title":"Sensitivity Graph"},{"category":"section","location":"vignettes/Smoking_Cessation_NHEFS/#Estimation-(Simulated-Data)","page":"Smoking Cessation NHEFS","text":"using DataFrames, Random\n\nRandom.seed!(1)\nn = 1566\nsex   = rand(0:1, n)\nrace  = rand(0:1, n)\nage   = rand(25:74, n)\nschool        = rand(6:17, n)\nsmokeintensity = rand(1:40, n)\nsmokeyrs      = rand(1:40, n)\nexercise      = rand(0:2, n)\nactive        = rand(0:2, n)\nwt71          = 55 .+ 20 .* randn(n)\nqsmk = Float64.(rand(n) .< 1 ./(1 .+ exp.(-(\n    -1.5 .+ 0.01 .* age .- 0.01 .* smokeintensity .- 0.01 .* smokeyrs))))\nwt82_71 = 2.5 .* qsmk .+ 0.02 .* (age .- 45) .+ 0.03 .* smokeintensity .+ randn(n) .* 5\n\ndata = DataFrame(sex=sex, race=race, age=age, school=school,\n                 smokeintensity=smokeintensity, smokeyrs=smokeyrs,\n                 exercise=exercise, active=active, wt71=wt71,\n                 qsmk=qsmk, wt82_71=wt82_71)\n\nres = estimate_causal(a=[1, 0], data=data, graph=graph,\n                      treatment=:qsmk, outcome=:wt82_71)\nr = x -> round(x, sigdigits=4)\n(ACE=r(res[:TMLE].ACE), lower_ci=r(res[:TMLE].lower_ci), upper_ci=r(res[:TMLE].upper_ci))\n\nThe true ACE in this simulation is 2.5 kg. On the real NHEFS data the TMLE estimate is approximately 3.5 kg (95% CI 2.4–4.5).","title":"Estimation (Simulated Data)"},{"category":"section","location":"reference/#Reference","page":"Reference","text":"","title":"Reference"},{"category":"function","location":"reference/#CausalGraphs.draw_graph","page":"Reference","text":"draw_graph(graph; direction=\"LR\")\n\nReturn a displayable graph object for an ADMG or MDAG.\n\nUses Graphviz (dot) to render an SVG when available — the SVG is embedded directly in HTML with no JavaScript dependency. Falls back to a Mermaid source object for environments without dot installed.\n\n\n\n\n\n","title":"CausalGraphs.draw_graph"},{"category":"function","location":"reference/#CausalGraphs.to_mermaid","page":"Reference","text":"to_mermaid(graph; direction=\"LR\")\n\nReturn a Mermaid flowchart representation of an ADMG.\n\nThis is convenient in Quarto, Markdown, and notebook contexts that support Mermaid diagrams.\n\n\n\n\n\n","title":"CausalGraphs.to_mermaid"},{"category":"function","location":"reference/#CausalGraphs.to_dot","page":"Reference","text":"to_dot(graph; direction=\"LR\")\n\nReturn a Graphviz DOT representation of an ADMG.\n\nDirected edges are blue. Bidirected edges, which represent hidden common causes, are red and drawn with arrowheads at both ends. Fixed vertices are drawn with square node shapes.\n\n\n\n\n\n","title":"CausalGraphs.to_dot"},{"category":"function","location":"reference/#CausalGraphs.ID_algorithm","page":"Reference","text":"ID_algorithm(g::ADMG, treatment, outcome)\n\nRun the Pearl-Shpitser ID algorithm for the ADMG total-effect query P(outcome | do(treatment)).\n\nThe return value is an ADMGIDResult. If identified == true, expression contains a symbolic identifying functional. If identification fails, hedge records the two district sets involved in the failure witness.\n\n\n\n\n\n","title":"CausalGraphs.ID_algorithm"},{"category":"function","location":"reference/#CausalGraphs.estimate_id","page":"Reference","text":"estimate_id(; a, data, graph, treatment, outcome, [max_levels])\n\nRun the general ADMG ID_algorithm and estimate the resulting symbolic functional with the finite-support plug-in estimator.\n\nThe return value uses the :IDPlugin key. This is an automatic estimator for discrete ID functionals, not a TMLE/EIF estimator for arbitrary ADMGs.\n\n\n\n\n\n","title":"CausalGraphs.estimate_id"},{"category":"function","location":"reference/#CausalGraphs.id_plugin_a","page":"Reference","text":"id_plugin_a(; a, data, graph, treatment, outcome, [id_result, max_levels])\n\nEstimate E[outcome(a)] from a symbolic ADMG ID expression using a finite-support plug-in estimator.\n\nThis estimator enumerates the observed support of the graph variables and evaluates the ID functional using empirical conditional probabilities. It is intended for discrete variables. For continuous variables, use one of the specialized semiparametric estimators when the graph is a-, p-, or nested-fixable.\n\n\n\n\n\n","title":"CausalGraphs.id_plugin_a"},{"category":"function","location":"reference/#CausalGraphs.fixing_sequence","page":"Reference","text":"fixing_sequence(g, nodes)\n\nTry to fix nodes by repeatedly applying the ADMG fixing criterion. Returns (fixable, fixing_order, graph).\n\n\n\n\n\n","title":"CausalGraphs.fixing_sequence"},{"category":"function","location":"reference/#CausalGraphs.nested_fixability","page":"Reference","text":"nested_fixability(g, treatment, outcome)\n\nRun the package's One-Line-ID style nested-fixability check for a total-effect query. The result includes the SWIG ancestor set ystar, the induced graph, the nested topological order, and per-district reachable-closure diagnostics.\n\n\n\n\n\n","title":"CausalGraphs.nested_fixability"},{"category":"function","location":"reference/#CausalGraphs.is_nested_fixable","page":"Reference","text":"is_nested_fixable(g, treatment, outcome)\n\nReturn true when nested_fixability(g, treatment, outcome) identifies the effect by the package's nested/fixing check.\n\n\n\n\n\n","title":"CausalGraphs.is_nested_fixable"},{"category":"function","location":"reference/#CausalGraphs.estimate_causal","page":"Reference","text":"estimate_causal(; a, data, graph=nothing, vertices, di_edges, bi_edges,\n                 treatment, outcome, [superlearner_Y, superlearner_A,\n                 crossfit, K, sample_weights, kwargs...])\n\nAuto-routing causal effect estimator for ADMGs.\n\nIdentification is determined automatically:\n\na-fixable (backdoor): returns TMLE, Onestep, Gcomp, IPW\np-fixable (front-door / NPS): returns TMLE, Onestep\nnested-fixable: returns ANIPW, NIPW\ngeneral ID-algorithm identifiable: returns a finite-support ID plug-in estimator for discrete variables\n\na may be a scalar for E[Y(a)] or a length-2 vector [a1, a0] for the ACE.\n\nExample — backdoor (a-fixable)\n\ng = make_graph(vertices=[:A,:Y,:X],\n               di_edges=[(:X,:A),(:X,:Y),(:A,:Y)])\nres = estimate_causal(a=[1,0], data=df, graph=g, treatment=:A, outcome=:Y)\nres[:TMLE].ACE\n\nExample — front-door (p-fixable)\n\ng = make_graph(vertices=[:A,:M,:Y],\n               bi_edges=[(:A,:Y)],\n               di_edges=[(:A,:M),(:M,:Y)])\nres = estimate_causal(a=[1,0], data=df, graph=g, treatment=:A, outcome=:Y)\n\nExample — nested-fixable (with SuperLearner)\n\nres = estimate_causal(a=[1,0], data=df, graph=g, treatment=:A, outcome=:Y,\n                      superlearner_Y=true, superlearner_A=true)\n\n\n\n\n\n","title":"CausalGraphs.estimate_causal"},{"category":"function","location":"reference/#CausalGraphs.superlearner","page":"Reference","text":"superlearner(; metalearner=nothing, binary=false)\n\nReturn an MLJ stacked ensemble (SuperLearner). binary=true for propensity score (classifier), binary=false for outcome regression.\n\n\n\n\n\n","title":"CausalGraphs.superlearner"},{"category":"function","location":"reference/#CausalGraphs.compute_missing_weights","page":"Reference","text":"compute_missing_weights(mdag, data; ID=nothing, law=:target,\n                        indicators=nothing, complete_cases_only=false,\n                        normalize=false, kwargs...)\n\nGiven a CausalGraphs.jl MDAG and a DataFrame with missing-indicator columns (0/1), run the missing-data identification algorithm and estimate propensity scores, then return inverse-probability weights for complete-case estimation.\n\nBy default the returned vector has one entry per row of data, with zero weight on incomplete rows. If complete_cases_only=true, only weights for rows where all requested indicators equal 1 are returned; this is useful when passing dropmissing(data) to estimate_causal.\n\nExample\n\nusing CausalGraphs, DataFrames\nmdag = make_mdag(\n    obs_variables=[\"A\",\"Y\"],\n    missing_variables=[\"X\"], missing_indicators=[\"Rx\"],\n    di_edges=[(\"A\",\"Y\"),(\"X\",\"A\"),(\"X\",\"Y\"),(\"X\",\"Rx\")]\n)\nID  = ID_algorithm(mdag)\nwts = compute_missing_weights(mdag, data; ID=ID, complete_cases_only=true)\nresult = estimate_causal(a=[1,0], data=dropmissing(data), ..., sample_weights=wts)\n\n\n\n\n\n","title":"CausalGraphs.compute_missing_weights"},{"category":"section","location":"#CausalGraphs.jl","page":"Home","text":"CausalGraphs.jl is a Julia package for graph-based causal identification, semiparametric effect estimation, and missing-data weighting in acyclic directed mixed graphs (ADMGs) and missingness DAGs (mDAGs). It includes estimator-routing checks for a-fixable, p-fixable, and nested-fixable effects, plus symbolic Pearl-Shpitser ID for more general ADMG queries and finite-support plug-in estimation for discrete ID functionals.\n\nIt brings together ideas and workflows from Anna Guo and Razieh Nabi's R packages flexCausal and flexMissing, rewritten and adapted for Julia.","title":"CausalGraphs.jl"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(url=\"https://github.com/xiangao/CausalGraphs.jl\")","title":"Installation"},{"category":"section","location":"#Quick-Start","page":"Home","text":"using CausalGraphs, DataFrames, Random\n\nRandom.seed!(1)\nn = 500\nX = randn(n)\nA = Float64.(rand(n) .< 1 ./ (1 .+ exp.(-X)))\nY = 2 .* A .+ X .+ randn(n)\ndata = DataFrame(X=X, A=A, Y=Y)\n\ngraph = make_graph(\n    vertices = [:X, :A, :Y],\n    di_edges = [(:X, :A), (:X, :Y), (:A, :Y)],\n)\n\nidentify(graph, :A, :Y).strategy\n\nresult = estimate_causal(\n    a = [1, 0],\n    data = data,\n    graph = graph,\n    treatment = :A,\n    outcome = :Y,\n)\n\nresult[:TMLE].ACE","title":"Quick Start"},{"category":"section","location":"#Vignettes","page":"Home","text":"Vignette Description\nGraphs and Identification ADMG construction, visualization, graph properties, and identify() routing\nGeneral ADMG ID Algorithm Symbolic Pearl-Shpitser ID, finite-support plug-in estimation, hedge failures, and fixing diagnostics\nEstimation: Backdoor and Front-Door Backdoor/a-fixable and front-door/p-fixable estimation workflows\nNested-Fixable and Missing Data Nested-fixable effects, ANIPW/NIPW, and missing-data weighting with mDAGs\nSmoking Cessation NHEFS End-to-end real-data workflow: hypothesize a graph, identify, estimate, and compare assumptions\nJob Training NSW Economics example: experimental assignment, measured selection, unmeasured selection, identification, and estimation\nJob Search Mediation JOBS II Economics mediation example: DAG, total-effect estimation, and natural direct and indirect effects","title":"Vignettes"},{"category":"section","location":"#Core-API","page":"Home","text":"make_graph\ndraw_graph\nto_mermaid\nidentify\nID_algorithm\nestimate_id\nestimate_causal\nmake_mdag\ncompute_missing_weights","title":"Core API"},{"category":"section","location":"#References","page":"Home","text":"Anna Guo and Razieh Nabi, flexCausal: causal effect estimation in ADMGs with hidden variables.\nAnna Guo and Razieh Nabi, flexMissing: weighting-based identification and estimation in graphical models of missing data.","title":"References"},{"category":"section","location":"vignettes/ADMG_ID_Algorithm/#General-ADMG-ID-Algorithm","page":"General ADMG ID Algorithm","text":"This vignette introduces the general ADMG identification tools in CausalGraphs.jl. identify() is the estimator-routing function: it checks the effect classes for which estimate_causal() has estimators. ID_algorithm() is the more general symbolic Pearl-Shpitser ID check for ADMG total-effect queries.","title":"General ADMG ID Algorithm"},{"category":"section","location":"vignettes/ADMG_ID_Algorithm/#Front-Door-as-ID","page":"General ADMG ID Algorithm","text":"The front-door graph has hidden treatment-outcome confounding, represented by A <-> Y, but the effect is still identified through the mediator M.\n\nusing CausalGraphs\n\ng_fd = make_graph(\n    vertices = [:A, :M, :Y],\n    di_edges = [(:A, :M), (:M, :Y)],\n    bi_edges = [(:A, :Y)],\n)\n\ndraw_graph(g_fd; direction=\"LR\")\n\nFor estimation, this graph is p-fixable, so identify() routes to the NPS-TMLE estimator.\n\nidentify(g_fd, :A, :Y).strategy\n\nThe same graph is also identified by the general ID algorithm:\n\nid_fd = ID_algorithm(g_fd, :A, :Y)\n(identified = id_fd.identified,\n expression = string(id_fd.expression))\n\nThe printed expression is intentionally compact. In the front-door graph it is the familiar formula: average the mediator distribution under the intervention, then average the mediator-outcome regression over the observed treatment distribution.","title":"Front-Door as ID"},{"category":"section","location":"vignettes/ADMG_ID_Algorithm/#Finite-Support-Plug-In-Estimation","page":"General ADMG ID Algorithm","text":"For discrete variables, estimate_id() can evaluate the symbolic ID functional automatically by enumerating the observed support and plugging in empirical conditional probabilities.\n\nusing DataFrames, Random\nRandom.seed!(24)\n\nn = 800\nA = Float64.(rand(n) .< 0.5)\nM = Float64.(rand(n) .< (0.2 .+ 0.6 .* A))\nY = Float64.(rand(n) .< (0.1 .+ 0.2 .* A .+ 0.4 .* M))\ndf = DataFrame(A=A, M=M, Y=Y)\n\nid_est = estimate_id(a=[1, 0], data=df, graph=g_fd, treatment=:A, outcome=:Y)\nid_est[:IDPlugin]\n\nThe same plug-in route is used by estimate_causal() when identify() returns :id_algorithm. The estimator is deliberately labeled :IDPlugin: it is an automatic finite-support estimator, not a general TMLE for arbitrary continuous ID functionals.","title":"Finite-Support Plug-In Estimation"},{"category":"section","location":"vignettes/ADMG_ID_Algorithm/#Non-Identification","page":"General ADMG ID Algorithm","text":"The bow graph has both A -> Y and hidden confounding A <-> Y. The observed association cannot separate the directed effect from the latent common cause.\n\ng_bow = make_graph(\n    vertices = [:A, :Y],\n    di_edges = [(:A, :Y)],\n    bi_edges = [(:A, :Y)],\n)\n\ndraw_graph(g_bow; direction=\"LR\")\n\nid_bow = ID_algorithm(g_bow, :A, :Y)\n(identified = id_bow.identified,\n hedge = id_bow.hedge)\n\nWhen ID fails, the returned hedge is a graph-theoretic witness of non-identification. In practice, this is more useful than a bare false because it points to the district structure responsible for the failure.","title":"Non-Identification"},{"category":"section","location":"vignettes/ADMG_ID_Algorithm/#Fixing-Sequences","page":"General ADMG ID Algorithm","text":"Fixing is the graph operation that turns random vertices into intervention-like or conditioned-on vertices by removing incoming directed edges and bidirected edges involving the fixed node. A vertex is fixable when it is not in the same district as one of its proper descendants.\n\ng_bd = make_graph(\n    vertices = [:X, :A, :Y],\n    di_edges = [(:X, :A), (:X, :Y), (:A, :Y)],\n)\n\nfixing_sequence(g_bd, [:A])\n\nFor the backdoor graph, fixing A succeeds immediately because A is not bidirected-connected to its descendant Y.","title":"Fixing Sequences"},{"category":"section","location":"vignettes/ADMG_ID_Algorithm/#Nested-Fixability-Diagnostics","page":"General ADMG ID Algorithm","text":"Nested-fixability uses fixing operations inside induced subgraphs. It is more general than ordinary backdoor adjustment and p-fixability, and it underlies the ANIPW/NIPW estimator route in this package.\n\ng_hiv = make_graph(\n    vertices = [:ViralLoad, :Income, :Exercise, :T, :Toxicity, :Adherence, :CD4],\n    di_edges = [\n        (:ViralLoad, :T), (:ViralLoad, :CD4), (:Income, :T), (:Exercise, :CD4),\n        (:T, :Toxicity), (:T, :CD4), (:Toxicity, :Adherence), (:Adherence, :CD4),\n    ],\n    bi_edges = [\n        (:Income, :Toxicity), (:Exercise, :Income), (:Exercise, :T),\n        (:ViralLoad, :Adherence), (:ViralLoad, :CD4),\n    ],\n)\n\nnested = nested_fixability(g_hiv, :T, :CD4)\n(a_fixable = is_fix(g_hiv, :T),\n p_fixable = is_p_fix(g_hiv, :T),\n nested_identified = nested.identified,\n ystar = nested.ystar,\n nested_order = nested.n_order)\n\nThe nested diagnostics show which outcome ancestors remain after fixing treatment and which districts must be reachable by fixing vertices outside the district.","title":"Nested-Fixability Diagnostics"},{"category":"section","location":"vignettes/ADMG_ID_Algorithm/#Practical-Boundary","page":"General ADMG ID Algorithm","text":"Use the functions at different levels of generality:\n\nis_fix() and is_p_fix() are simple graph checks.\nnested_fixability() explains the nested/fixing route used by the ANIPW estimator.\nID_algorithm() answers the more general symbolic ADMG identification question and returns a hedge when the effect is not identified.\nidentify() combines these checks for package routing.\nestimate_id() estimates symbolic ID functionals by finite-support plug-in when the variables are discrete.\nestimate_causal() estimates a-fixable, p-fixable, nested-fixable, and discrete ID-algorithm functionals.\n\nGeneral ID can produce complicated functionals. Those functionals are useful identification results. For continuous or high-dimensional variables, they still require specialized estimation machinery beyond finite support enumeration.","title":"Practical Boundary"},{"category":"section","location":"vignettes/Graphs_and_Identification/#Graphs-and-Identification","page":"Graphs and Identification","text":"This vignette introduces the graph layer and the identify() function.\n\nA graph is a compact statement of causal assumptions. It tells us which variables can directly affect other variables and which observed variables may share hidden common causes. Identification asks whether an interventional quantity, such as E[Y(a)] or E[Y(1)-Y(0)], can be written in terms of the observed data distribution under those assumptions.","title":"Graphs and Identification"},{"category":"section","location":"vignettes/Graphs_and_Identification/#Constructing-an-ADMG","page":"Graphs and Identification","text":"An acyclic directed mixed graph has directed edges for observed causal relationships and bidirected edges for latent common causes.\n\nIn an ADMG:\n\nA -> B means A may directly cause B;\nA <-> B means A and B share an unmeasured common cause;\ndirected arrows cannot form cycles.\n\nADMGs are useful because they represent latent confounding without drawing each unobserved variable. Instead of adding a hidden U with U -> A and U -> Y, we draw A <-> Y.\n\nusing CausalGraphs\n\ng_fd = make_graph(\n    vertices = [:A, :M, :Y],\n    di_edges = [(:A, :M), (:M, :Y)],\n    bi_edges = [(:A, :Y)],\n)\n\ndraw_graph(g_fd; direction=\"LR\")\n\ng_bd = make_graph(\n    vertices = [:X, :A, :Y],\n    di_edges = [(:X, :A), (:X, :Y), (:A, :Y)],\n)\n\ndraw_graph(g_bd; direction=\"LR\")","title":"Constructing an ADMG"},{"category":"section","location":"vignettes/Graphs_and_Identification/#Graph-Properties","page":"Graphs and Identification","text":"Graph properties are the vocabulary used by identification algorithms. Parents and children describe direct arrows. Ancestors and descendants describe all directed upstream or downstream variables. Districts are bidirected-connected components and encode latent confounding. Markov pillows are local adjustment sets used by the a-fixable/backdoor estimator.\n\nparents(g_fd, :M)\n\nchildren(g_fd, :A)\n\ndistrict(g_fd, :A)\n\nmarkov_pillow(g_bd, :A; treatment=:A)","title":"Graph Properties"},{"category":"section","location":"vignettes/Graphs_and_Identification/#Identification","page":"Graphs and Identification","text":"identify() routes the graph to the implemented estimation strategy.\n\nidentify(g_bd, :A, :Y).strategy\n\nidentify(g_fd, :A, :Y).strategy\n\nThe implemented strategies are:\n\nStrategy Interpretation\n:a_fixable Backdoor/a-fixable effect\n:p_fixable Front-door, primal-fixable, or NPS effect\n:nested_fixable Identified by a One-Line-ID style nested check\n:id_algorithm Identified by the general Pearl-Shpitser ID algorithm; finite-support plug-in estimation is available for discrete variables\n:not_identified Not identified by the implemented criteria\n\nThese strategies correspond to different identification arguments:\n\n:a_fixable covers backdoor-style effects. The treatment can be adjusted using its Markov pillow.\n:p_fixable covers front-door/primal-fixable/NPS effects. The treatment may be confounded with the outcome, but its children are not in the treatment's hidden-confounding district.\n:nested_fixable covers ADMGs where neither simple adjustment nor p-fixability is enough, but a One-Line ID style fixing sequence still identifies the effect.\n:id_algorithm covers more general ADMGs where the recursive ID algorithm finds a symbolic functional. The package can estimate such functionals with a finite-support plug-in estimator when the variables are discrete.\n\nThe returned strategy is an estimation instruction for estimate_causal() when it is :a_fixable, :p_fixable, :nested_fixable, or :id_algorithm. For :id_algorithm, inspect identify(...).id_expression, call ID_algorithm() directly, or use estimate_id() for the finite-support plug-in estimator.\n\nFor the backdoor graph, the a-fixability condition holds because A is not in the same hidden-confounding district as any descendant other than itself. The Markov pillow is X, which becomes the adjustment set.\n\n(descendants_A = descendants(g_bd, [:A]),\n district_A = district(g_bd, :A),\n markov_pillow_A = markov_pillow(g_bd, :A; treatment=:A))\n\nIf a graph has both A -> Y and A <-> Y, the directed causal effect and the hidden common cause are entangled. That bow graph is the simplest non-identified case.","title":"Identification"},{"category":"section","location":"vignettes/Graphs_and_Identification/#General-ID-Algorithm","page":"Graphs and Identification","text":"ID_algorithm() runs the Pearl-Shpitser recursive ID algorithm for ADMG queries. It is useful for separating mathematical identification from estimator routing. On the front-door graph, it recovers the familiar front-door functional:\n\nid_general = ID_algorithm(g_fd, :A, :Y)\n(identified = id_general.identified,\n expression = string(id_general.expression))\n\nOn the bow graph, the general ID algorithm fails and returns a hedge witness.\n\ng_bow = make_graph(\n    vertices = [:A, :Y],\n    di_edges = [(:A, :Y)],\n    bi_edges = [(:A, :Y)],\n)\n\nid_bow = ID_algorithm(g_bow, :A, :Y)\n(identified = id_bow.identified, hedge = id_bow.hedge)","title":"General ID Algorithm"}]
}
